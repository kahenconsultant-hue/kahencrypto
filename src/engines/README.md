# C.M.I.P Engines

Phase 2 intentionally does not build or activate advanced intelligence engines.

This folder is reserved for production engine modules that will consume persisted
`raw_events`, `raw_metrics`, `market_prices`, source health, and reliability
snapshots. Existing prototype analytics under `src/server/analytics` should be
migrated here only after the ingestion foundation and storage contracts are
stable.
